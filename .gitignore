# Round 44 Step 202 — MAC vendor lookup Phase 2B (doc/macvendor.md §七 #1).
# oui.json.gz is generated by .github/workflows/build.yml from Wireshark's
# manuf community database. NEVER commit it — keeps git history clean,
# ensures the IPK always ships the latest registrations.
htdocs/luci-static/design-x/data/oui.json.gz

# Build artifacts
sdk/
*.ipk
*.tar
*.tar.gz

# Local dev / IDE
.DS_Store
.idea/
.vscode/
*.swp
*.swo

# Claude / agent local state (uncommitted)
.claude/
.claude-flow/
.mcp.json
CLAUDE.md
